apiVersion: v1
kind: Service
metadata:
  name: mercury-docs
  namespace: {{ .Values.global.namespace }}
  labels:
    role: mercury-docs
spec:
  ports:
  - port: 8960
    targetPort: 8960
    protocol: TCP
  selector:
    role: mercury-docs

---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    role: mercury-docs
    app: aml
  name: mercury-docs
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    matchLabels:
      role: mercury-docs
      app: aml
  replicas: {{  .Values.replicas }}
  template:
    metadata:
      labels:
        role: mercury-docs
        app: aml
    spec:
      imagePullSecrets:
      - name: docker-io
      containers:
      - name: mercury-docs
        image: redpointglobal/mercury_docs:{{ .Values.images.tag }}
        ports:
        - name: mercury-docs
          containerPort: 8960
      restartPolicy: Always
