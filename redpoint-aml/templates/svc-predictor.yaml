# predictor service
apiVersion: v1
kind: Service
metadata:
  name: predictor
  namespace: {{ .Values.global.namespace }}
  labels:
    role: predictor
spec:
  ports:
  - port: 11000
    protocol: TCP
  selector:
    role: predictor
