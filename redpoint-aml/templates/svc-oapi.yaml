# Redis cache service
apiVersion: v1
kind: Service
metadata:
  name: oapi
  namespace: {{ .Values.application.namespace }}
  labels:
    role: oapi
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    role: oapi