# Redis cache service
apiVersion: v1
kind: Service
metadata:
  name: cache
  namespace: {{ .Values.application.namespace }}
  labels:
    role: cache
spec:
  ports:
  - port: 6379
    protocol: TCP
  selector:
    role: cache
